using System;
using UserMetods.Methods;// se incluye con el using en namespace para usar la clase definida en otro archivo

namespace _020._namespace_and_using
{
    class Program
    {
        static void Main(string[] args)
        {
            var nu = new UserMetods(); // instanciamos la clase que traemos desde el namespace agregado con el using
        }
    }
}
